# 햅틱 드라이버 시스템 — 팀원용 간단 가이드

> 이 문서는 상세 치트시트의 요약본입니다.  
> 자세한 레지스터 값/공식은 `DRV2605L_Cheat_Sheet.md` 참조

---

## 우리 시스템이 뭔가요?

소리를 진동으로 바꿔서 몸에 전달하는 장치입니다.

```
소리 → ESP32(두뇌) → 드라이버 8개 → 모터 8개 → 진동!
```

---

## 부품 한눈에 보기

| 부품 | 역할 | 비유 |
|---|---|---|
| **ESP32** | 메인 컨트롤러 | 🧠 두뇌. 어떤 모터를 얼마나 떨릴지 결정 |
| **TCA9548A** | I²C 멀티플렉서 | 🔀 전화 교환기. ESP32가 8개 드라이버와 1:1 통화 가능하게 해줌 |
| **DRV2605L** | 햅틱 드라이버 (×8) | ⚡ 증폭기. "진폭 120으로 떨어" 명령을 받아 모터에 전력 공급 |
| **LRA 모터** | 진동 액추에이터 (×8) | 📳 스피커 같은 것. 전기 → 기계적 진동 변환 |

---

## 통신 구조

```
ESP32
  │  I²C 버스 (2개 선: SDA, SCL)
  │
  ▼
TCA9548A (주소: 0x70)  ← "3번 채널 열어줘"
  │
  ├── CH0 → DRV2605L → 모터 (170Hz)
  ├── CH1 → DRV2605L → 모터 (170Hz)
  ├── CH2 → DRV2605L → 모터 (170Hz)
  ├── CH3 → DRV2605L → 모터 (170Hz)
  ├── CH4 → DRV2605L → 모터 (230Hz)
  ├── CH5 → DRV2605L → 모터 (230Hz)
  ├── CH6 → DRV2605L → 모터 (230Hz)
  └── CH7 → DRV2605L → 모터 (230Hz)
```

**왜 멀티플렉서가 필요한가?**  
DRV2605L의 I²C 주소가 0x5A로 고정이라 같은 버스에 여러 개를 못 붙임.  
TCA9548A가 채널을 바꿔주면 ESP32는 한 번에 하나씩만 통신하므로 충돌 없음.

---

## DRV2605L 핵심 개념 (3가지만 기억하세요)

### 1. 모드 — "드라이버야, 뭘 할 거야?"

| 모드 | 한 줄 설명 | 우리가 쓰는가? |
|---|---|---|
| **RTP** (모드 5) | 실시간으로 진폭 값을 직접 보냄 | ✅ **주력** |
| Library (모드 0) | 내장된 123개 패턴 중 골라서 재생 | ⭕ 보조 |
| Auto Cal (모드 7) | 모터 특성에 맞게 자동 보정 | ⭕ 초기화 시 1회 |

### 2. 진폭 — "얼마나 세게 떨어?"

RTP 모드에서 0x02 레지스터에 값을 쓰면 바로 진동:

```
0   = 정지 (진동 없음)
64  = 25% 세기
128 = 50% 세기
200 = ~78% 세기
255 = 최대 (풀파워)
```

### 3. 캘리브레이션 — "모터야, 너 특성이 뭐야?"

부팅할 때 한 번 실행. 모터의 물리적 특성(임피던스, BEMF)을 측정해서 드라이버가 최적으로 구동할 수 있게 함.

---

## 우리 모터 2종류

| | VG1040003D | VLV221007E |
|---|---|---|
| 공진주파수 | **170Hz** (낮은 웅웅) | **230Hz** (높은 윙윙) |
| 정격 전압 | 2.5V | 2.3V |
| 반응 속도 | **빠름** (10ms) | 보통 (50ms) |
| 느낌 | 강하고 묵직한 진동 | 섬세하고 가벼운 진동 |
| 용도 | 저음/충격 표현 | 고음/텍스처 표현 |

---

## 코드에서 쓰는 핵심 함수 3개

```cpp
// 1. 채널 선택 (어떤 드라이버와 통신할지)
tcaSelect(3);        // CH3의 DRV2605L과 통신 시작

// 2. 진폭 설정 (얼마나 세게)
drvWrite(0x02, 200); // 진폭 200으로 진동 시작

// 3. 정지
drvWrite(0x02, 0);   // 진동 멈춤
```

### 전체 흐름 예시

```cpp
// CH0 모터를 200 세기로 100ms 진동
tcaSelect(0);           // CH0 선택
drvWrite(0x02, 200);    // 진동 시작
delay(100);             // 100ms 유지
drvWrite(0x02, 0);      // 정지

// CH4 모터를 150 세기로 50ms 진동
tcaSelect(4);           // CH4 선택
drvWrite(0x02, 150);    // 진동 시작
delay(50);              // 50ms 유지
drvWrite(0x02, 0);      // 정지
```

---

## 내장 이펙트 사용법 (Library 모드)

RTP 대신 미리 만들어진 진동 패턴을 재생할 수도 있습니다:

```cpp
tcaSelect(0);              // 채널 선택
drvWrite(0x01, 0x00);      // Library 모드
drvWrite(0x03, 0x06);      // LRA 라이브러리
drvWrite(0x04, 1);         // 이펙트 #1 (Strong Click)
drvWrite(0x05, 0);         // 끝
drvWrite(0x0C, 0x01);      // 재생!
```

### 자주 쓸 만한 이펙트

| ID | 이름 | 느낌 |
|---|---|---|
| 1 | Strong Click | 톡! 하는 강한 클릭 |
| 7 | Soft Bump | 부드러운 탭 |
| 10 | Double Click | 톡톡! 더블 클릭 |
| 14 | Sharp Tick 80% | 째깍 하는 틱 |
| 47 | Alert Pattern 5 | 알림 진동 |
| 52 | Medium Impact | 쿵 하는 충격 |
| 119~123 | Smooth Hum | 지속적인 윙~ 진동 |

> 전체 123개 목록: `DRV2605L_Effect_Library.md` 참조

---

## 트러블슈팅 (문제가 생기면)

### "모터가 안 떨려요"

```
1. ESP32 시리얼 모니터에서 I²C 스캔 → 0x70(TCA) 보이는지 확인
2. tcaSelect(채널) 후 다시 스캔 → 0x5A(DRV) 보이는지 확인
3. 안 보이면: 배선 확인 (SDA↔SDA, SCL↔SCL 맞는지)
4. 보이는데 안 떨리면: 캘리브레이션 실패일 수 있음
   → Status 레지스터(0x00) 읽어서 0xE0인지 확인
   → 0xE8이면 캘리 실패 → DRIVE_TIME 값이 모터 주파수와 맞는지 확인
```

### "진동이 약해요"

```
1. OD_CLAMP 값 확인 (너무 낮으면 최대 출력이 제한됨)
2. VDD 전압 확인 (3.3V보다 5V에서 더 세게 구동 가능)
3. unsigned 모드 사용 중인지 확인 (signed면 0~127까지만 유효)
```

### "특정 채널만 안 돼요"

```
1. 해당 채널의 DRV2605L 보드 결선 확인
2. 해당 채널의 풀업 저항 확인 (4.7kΩ)
3. 모터 커넥터 접촉 확인 (OUT+/OUT-)
```

---

## 용어 정리

| 용어 | 뜻 |
|---|---|
| **I²C** | 2개 선(SDA, SCL)으로 여러 칩이 통신하는 프로토콜 |
| **SDA** | 데이터 선 |
| **SCL** | 클럭(타이밍) 선 |
| **RTP** | Real-Time Playback. 진폭을 실시간으로 직접 제어 |
| **LRA** | Linear Resonance Actuator. 선형 공진 진동 모터 |
| **ERM** | Eccentric Rotating Mass. 편심 회전 모터 (우리는 안 씀) |
| **캘리브레이션** | 모터 특성 자동 측정/보정 |
| **BEMF** | Back-EMF. 모터가 돌면서 발생하는 역기전력 |
| **Closed-Loop** | BEMF 피드백으로 정밀 제어 (우리 방식) |
| **Open-Loop** | 피드백 없이 구동 (간단하지만 정밀도↓) |
| **MUX** | Multiplexer. 여러 채널 중 하나를 선택하는 스위치 |
| **OTP** | One-Time Programmable. 한 번만 쓸 수 있음 (절대 건드리지 말 것!) |

---

*상세 기술 자료: `DRV2605L_Cheat_Sheet.md`, `TCA9548A_Cheat_Sheet.md`, `Hardware_Setup_Cheat_Sheet.md`*
